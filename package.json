{
  "name": "sentinel",
  "version": "0.1.0",
  "description": "Safe execution infrastructure for blockchain — transaction guard, execution patterns, and Jito bundle management",
  "private": true,
  "scripts": {
    "prepare": "pnpm run build",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "pnpm --filter @sentinel/qn-addon dev"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.31.0"
}
