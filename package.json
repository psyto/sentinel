{
  "name": "sentinel",
  "version": "0.1.0",
  "description": "Safe execution infrastructure for blockchain — transaction guard, execution patterns, and Jito bundle management",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm -w packages/core run build && npm -w packages/qn-addon run build",
    "test": "npm -w packages/core run test",
    "dev": "npm -w packages/qn-addon run dev"
  },
  "license": "MIT"
}
